<MudDialog>
    <DialogContent>
        <div class="buttons">
            @foreach (var d in Feedrate.Feedrates)
            {
                <div>
                    <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick='() => SelectFeedrate(d)' FullWidth="true">@d</MudButton>
                </div>
            }
        </div>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Primary" OnClick="Close">Ok</MudButton>
    </DialogActions>
</MudDialog>